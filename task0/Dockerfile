# Use the latest Ubuntu base image
FROM ubuntu:latest

# Update APT and upgrade all software
RUN apt-get update && apt-get upgrade -y

# Set the command to print "Hello, World!" to the terminal
CMD ["echo", "Hello, World!"]
